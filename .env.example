# Set to True to use MySQL database, or False to fallback to local SQLite
USE_MYSQL=True

# MySQL database configuration
DB_NAME=bargain_ai
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_HOST=127.0.0.1
DB_PORT=3306
